"""Domain layer for Plate Guard.

Contains pure business logic: entities, value objects, domain events,
and repository / service interfaces (Protocols). This layer has zero
external dependencies beyond the Python standard library.
"""
